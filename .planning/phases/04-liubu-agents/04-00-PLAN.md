---
phase: 04-liubu-agents
plan: 00
type: infrastructure
wave: 0
files_modified:
  - tests/agents/test_libu.py
  - tests/agents/test_gongbu.py
  - tests/agents/test_xingbu.py
  - tests/agents/test_libu2.py
  - tests/agents/test_duchayuan.py
requirements: [AGT-02, AGT-03, AGT-04, AGT-05, AGT-06]
autonomous: true
must_haves:
  truths:
    - "Test files created with skip markers for unimplemented features"
    - "Tests follow existing agent test patterns"
  artifacts:
    - path: "tests/agents/test_libu.py"
      provides: "Test infrastructure for Libu Agent"
    - path: "tests/agents/test_gongbu.py"
      provides: "Test infrastructure for Gongbu Agent"
    - path: "tests/agents/test_xingbu.py"
      provides: "Test infrastructure for Xingbu Agent"
    - path: "tests/agents/test_libu2.py"
      provides: "Test infrastructure for Libu2 (礼部) Agent"
    - path: "tests/agents/test_duchayuan.py"
      provides: "Test infrastructure for Duchayuan Agent"
---

<objective>
Create test infrastructure for Phase 4 agents (六部 Agent).

Purpose: Set up test files with proper structure before implementing agents.
Output: 5 test files with skip markers for unimplemented features.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From tests/agents/test_bingbu.py:
```python
class TestBingbuState:
    """Tests for BingbuState structure."""
    def test_bingbu_state_has_required_fields(self):
        from src.agents.bingbu import BingbuState
        # ...

class TestBingbuTools:
    """Tests for Bingbu Agent tools."""
    def test_bingbu_tools_defined(self):
        pytest.skip("bingbu_tools not yet implemented")

class TestBingbuGraph:
    """Tests for Bingbu Agent graph."""
    def test_build_bingbu_graph(self):
        pytest.skip("build_bingbu_graph not yet implemented")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Libu Agent tests</name>
  <files>tests/agents/test_libu.py</files>
  <action>
    Create test file for Libu (吏部) Agent - skill management.

    ```python
    """
    Tests for AGT-02: Libu Agent implementation.

    Libu (吏部) - Ministry of Personnel
    Responsibilities: Skill management, installation, updates
    """

    import pytest


    class TestLibuState:
        """Tests for LibuState structure."""

        def test_libu_state_has_required_fields(self):
            """LibuState must have required fields."""
            pytest.skip("LibuState not yet implemented (Plan 01)")

        def test_libu_state_tracking_fields(self):
            """LibuState must track skills_installed, skills_updated."""
            pytest.skip("LibuState not yet implemented (Plan 01)")


    class TestLibuTools:
        """Tests for Libu Agent tools."""

        def test_libu_tools_defined(self):
            """Libu tools must be defined."""
            pytest.skip("libu_tools not yet implemented (Plan 01)")

        def test_tool_install_skill_exists(self):
            """tool_install_skill must exist."""
            pytest.skip("tool_install_skill not yet implemented (Plan 01)")

        def test_tool_update_skill_exists(self):
            """tool_update_skill must exist."""
            pytest.skip("tool_update_skill not yet implemented (Plan 01)")

        def test_tool_list_skills_exists(self):
            """tool_list_skills must exist."""
            pytest.skip("tool_list_skills not yet implemented (Plan 01)")


    class TestLibuGraph:
        """Tests for Libu Agent graph."""

        def test_build_libu_graph(self):
            """build_libu_graph must return compiled graph."""
            pytest.skip("build_libu_graph not yet implemented (Plan 01)")

        def test_libu_graph_has_react_node(self):
            """Libu graph must have react node."""
            pytest.skip("build_libu_graph not yet implemented (Plan 01)")

        def test_libu_graph_has_tools_node(self):
            """Libu graph must have tools node."""
            pytest.skip("build_libu_graph not yet implemented (Plan 01)")
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/agents/test_libu.py --collect-only</automated>
  </verify>
  <done>Libu test file created with skip markers</done>
</task>

<task type="auto">
  <name>Task 2: Create Gongbu Agent tests</name>
  <files>tests/agents/test_gongbu.py</files>
  <action>
    Create test file for Gongbu (工部) Agent - environment management.

    ```python
    """
    Tests for AGT-03: Gongbu Agent implementation.

    Gongbu (工部) - Ministry of Works
    Responsibilities: Environment maintenance, sandbox management
    """

    import pytest


    class TestGongbuState:
        """Tests for GongbuState structure."""

        def test_gongbu_state_has_required_fields(self):
            """GongbuState must have required fields."""
            pytest.skip("GongbuState not yet implemented (Plan 02)")

        def test_gongbu_state_tracking_fields(self):
            """GongbuState must track environments_created, commands_run."""
            pytest.skip("GongbuState not yet implemented (Plan 02)")


    class TestGongbuTools:
        """Tests for Gongbu Agent tools."""

        def test_gongbu_tools_defined(self):
            """Gongbu tools must be defined."""
            pytest.skip("gongbu_tools not yet implemented (Plan 02)")

        def test_tool_create_env_exists(self):
            """tool_create_env must exist."""
            pytest.skip("tool_create_env not yet implemented (Plan 02)")

        def test_tool_run_command_exists(self):
            """tool_run_command must exist."""
            pytest.skip("tool_run_command not yet implemented (Plan 02)")

        def test_tool_check_env_exists(self):
            """tool_check_env must exist."""
            pytest.skip("tool_check_env not yet implemented (Plan 02)")


    class TestGongbuGraph:
        """Tests for Gongbu Agent graph."""

        def test_build_gongbu_graph(self):
            """build_gongbu_graph must return compiled graph."""
            pytest.skip("build_gongbu_graph not yet implemented (Plan 02)")

        def test_gongbu_graph_has_react_node(self):
            """Gongbu graph must have react node."""
            pytest.skip("build_gongbu_graph not yet implemented (Plan 02)")

        def test_gongbu_graph_has_tools_node(self):
            """Gongbu graph must have tools node."""
            pytest.skip("build_gongbu_graph not yet implemented (Plan 02)")
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/agents/test_gongbu.py --collect-only</automated>
  </verify>
  <done>Gongbu test file created with skip markers</done>
</task>

<task type="auto">
  <name>Task 3: Create Xingbu Agent tests</name>
  <files>tests/agents/test_xingbu.py</files>
  <action>
    Create test file for Xingbu (刑部) Agent - code review and testing.

    ```python
    """
    Tests for AGT-04: Xingbu Agent implementation.

    Xingbu (刑部) - Ministry of Justice
    Responsibilities: Code review, testing, quality assurance
    """

    import pytest


    class TestXingbuState:
        """Tests for XingbuState structure."""

        def test_xingbu_state_has_required_fields(self):
            """XingbuState must have required fields."""
            pytest.skip("XingbuState not yet implemented (Plan 03)")

        def test_xingbu_state_tracking_fields(self):
            """XingbuState must track files_reviewed, tests_run, issues_found."""
            pytest.skip("XingbuState not yet implemented (Plan 03)")


    class TestXingbuTools:
        """Tests for Xingbu Agent tools."""

        def test_xingbu_tools_defined(self):
            """Xingbu tools must be defined."""
            pytest.skip("xingbu_tools not yet implemented (Plan 03)")

        def test_tool_review_code_exists(self):
            """tool_review_code must exist."""
            pytest.skip("tool_review_code not yet implemented (Plan 03)")

        def test_tool_run_tests_exists(self):
            """tool_run_tests must exist."""
            pytest.skip("tool_run_tests not yet implemented (Plan 03)")

        def test_tool_check_coverage_exists(self):
            """tool_check_coverage must exist."""
            pytest.skip("tool_check_coverage not yet implemented (Plan 03)")


    class TestXingbuGraph:
        """Tests for Xingbu Agent graph."""

        def test_build_xingbu_graph(self):
            """build_xingbu_graph must return compiled graph."""
            pytest.skip("build_xingbu_graph not yet implemented (Plan 03)")

        def test_xingbu_graph_has_react_node(self):
            """Xingbu graph must have react node."""
            pytest.skip("build_xingbu_graph not yet implemented (Plan 03)")

        def test_xingbu_graph_has_tools_node(self):
            """Xingbu graph must have tools node."""
            pytest.skip("build_xingbu_graph not yet implemented (Plan 03)")
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/agents/test_xingbu.py --collect-only</automated>
  </verify>
  <done>Xingbu test file created with skip markers</done>
</task>

<task type="auto">
  <name>Task 4: Create Libu2 (礼部) Agent tests</name>
  <files>tests/agents/test_libu2.py</files>
  <action>
    Create test file for Libu2 (礼部) Agent - documentation.

    ```python
    """
    Tests for AGT-05: Libu2 Agent implementation.

    Libu2 (礼部) - Ministry of Rites
    Responsibilities: Documentation generation, formatting
    """

    import pytest


    class TestLibu2State:
        """Tests for Libu2State structure."""

        def test_libu2_state_has_required_fields(self):
            """Libu2State must have required fields."""
            pytest.skip("Libu2State not yet implemented (Plan 04)")

        def test_libu2_state_tracking_fields(self):
            """Libu2State must track docs_created, docs_updated."""
            pytest.skip("Libu2State not yet implemented (Plan 04)")


    class TestLibu2Tools:
        """Tests for Libu2 Agent tools."""

        def test_libu2_tools_defined(self):
            """Libu2 tools must be defined."""
            pytest.skip("libu2_tools not yet implemented (Plan 04)")

        def test_tool_generate_doc_exists(self):
            """tool_generate_doc must exist."""
            pytest.skip("tool_generate_doc not yet implemented (Plan 04)")

        def test_tool_format_markdown_exists(self):
            """tool_format_markdown must exist."""
            pytest.skip("tool_format_markdown not yet implemented (Plan 04)")


    class TestLibu2Graph:
        """Tests for Libu2 Agent graph."""

        def test_build_libu2_graph(self):
            """build_libu2_graph must return compiled graph."""
            pytest.skip("build_libu2_graph not yet implemented (Plan 04)")

        def test_libu2_graph_has_react_node(self):
            """Libu2 graph must have react node."""
            pytest.skip("build_libu2_graph not yet implemented (Plan 04)")

        def test_libu2_graph_has_tools_node(self):
            """Libu2 graph must have tools node."""
            pytest.skip("build_libu2_graph not yet implemented (Plan 04)")
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/agents/test_libu2.py --collect-only</automated>
  </verify>
  <done>Libu2 test file created with skip markers</done>
</task>

<task type="auto">
  <name>Task 5: Create Duchayuan Agent tests</name>
  <files>tests/agents/test_duchayuan.py</files>
  <action>
    Create test file for Duchayuan (都察院) Agent - security audit.

    ```python
    """
    Tests for AGT-06: Duchayuan Agent implementation.

    Duchayuan (都察院) - Censorate
    Responsibilities: Security audit, vulnerability detection
    """

    import pytest


    class TestDuchayuanState:
        """Tests for DuchayuanState structure."""

        def test_duchayuan_state_has_required_fields(self):
            """DuchayuanState must have required fields."""
            pytest.skip("DuchayuanState not yet implemented (Plan 05)")

        def test_duchayuan_state_tracking_fields(self):
            """DuchayuanState must track audits_run, vulnerabilities_found."""
            pytest.skip("DuchayuanState not yet implemented (Plan 05)")


    class TestDuchayuanTools:
        """Tests for Duchayuan Agent tools."""

        def test_duchayuan_tools_defined(self):
            """Duchayuan tools must be defined."""
            pytest.skip("duchayuan_tools not yet implemented (Plan 05)")

        def test_tool_security_scan_exists(self):
            """tool_security_scan must exist."""
            pytest.skip("tool_security_scan not yet implemented (Plan 05)")

        def test_tool_check_secrets_exists(self):
            """tool_check_secrets must exist."""
            pytest.skip("tool_check_secrets not yet implemented (Plan 05)")


    class TestDuchayuanGraph:
        """Tests for Duchayuan Agent graph."""

        def test_build_duchayuan_graph(self):
            """build_duchayuan_graph must return compiled graph."""
            pytest.skip("build_duchayuan_graph not yet implemented (Plan 05)")

        def test_duchayuan_graph_has_react_node(self):
            """Duchayuan graph must have react node."""
            pytest.skip("build_duchayuan_graph not yet implemented (Plan 05)")

        def test_duchayuan_graph_has_tools_node(self):
            """Duchayuan graph must have tools node."""
            pytest.skip("build_duchayuan_graph not yet implemented (Plan 05)")
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/agents/test_duchayuan.py --collect-only</automated>
  </verify>
  <done>Duchayuan test file created with skip markers</done>
</task>

</tasks>

<verification>
Run all test files:
```bash
python -m pytest tests/agents/test_libu.py tests/agents/test_gongbu.py tests/agents/test_xingbu.py tests/agents/test_libu2.py tests/agents/test_duchayuan.py --collect-only
```

Expected: All tests collected with skip markers.
</verification>

<success_criteria>
- [ ] tests/agents/test_libu.py created with skip markers
- [ ] tests/agents/test_gongbu.py created with skip markers
- [ ] tests/agents/test_xingbu.py created with skip markers
- [ ] tests/agents/test_libu2.py created with skip markers
- [ ] tests/agents/test_duchayuan.py created with skip markers
- [ ] All tests can be collected by pytest
</success_criteria>

<output>
After completion, create `.planning/phases/04-liubu-agents/04-00-SUMMARY.md`
</output>
