---
phase: 07-memory-system
plan: 01
type: execute
requirements: [MEM-01]
files_modified:
  - src/tools/user_info_collector.py
  - tests/memory/test_user_info_collector.py
---

<objective>
Implement user information collection tool.

Purpose: Allow Hubu agent to collect user info directly without going through Zhongshu.
Output: User info collection tools for demographics, preferences, and context.
</objective>

<tasks>

<task type="auto">
  <name>Create user_info_collector.py</name>
  <files>src/tools/user_info_collector.py</files>
  <action>
    Create user info collection tools:

    ```python
    """
    User information collection tools.

    Implements MEM-01: User info collection as independent tool
    """

    import json
    from typing import Dict, Any, List, Optional
    from dataclasses import dataclass, field, asdict
    from datetime import datetime
    import logging

    logger = logging.getLogger(__name__)


    @dataclass
    class UserProfile:
        """User profile information."""
        user_id: str
        name: Optional[str] = None
        email: Optional[str] = None
        timezone: Optional[str] = None
        language: str = "zh-CN"
        preferences: Dict[str, Any] = field(default_factory=dict)
        created_at: str = field(default_factory=lambda: datetime.utcnow().isoformat())
        updated_at: str = field(default_factory=lambda: datetime.utcnow().isoformat())


    @dataclass
    class ConversationContext:
        """Context for current conversation."""
        session_id: str
        topic: Optional[str] = None
        goals: List[str] = field(default_factory=list)
        entities: Dict[str, Any] = field(default_factory=dict)
        sentiment: str = "neutral"
        created_at: str = field(default_factory=lambda: datetime.utcnow().isoformat())


    class UserInfoCollector:
        """Collects and manages user information."""

        def __init__(self):
            self._profiles: Dict[str, UserProfile] = {}
            self._contexts: Dict[str, ConversationContext] = {}

        def collect_basic_info(
            self,
            user_id: str,
            name: str = None,
            email: str = None,
            timezone: str = None
        ) -> str:
            """
            Collect basic user information.

            Args:
                user_id: Unique user identifier
                name: User's name
                email: User's email
                timezone: User's timezone

            Returns:
                Confirmation message
            """
            if user_id not in self._profiles:
                self._profiles[user_id] = UserProfile(user_id=user_id)

            profile = self._profiles[user_id]
            if name:
                profile.name = name
            if email:
                profile.email = email
            if timezone:
                profile.timezone = timezone
            profile.updated_at = datetime.utcnow().isoformat()

            logger.info(f"Updated profile for user {user_id}")
            return f"Collected basic info for {user_id}: name={name}, email={email}"

        def set_preference(
            self,
            user_id: str,
            key: str,
            value: Any
        ) -> str:
            """
            Set a user preference.

            Args:
                user_id: User identifier
                key: Preference key
                value: Preference value

            Returns:
                Confirmation message
            """
            if user_id not in self._profiles:
                self._profiles[user_id] = UserProfile(user_id=user_id)

            self._profiles[user_id].preferences[key] = value
            self._profiles[user_id].updated_at = datetime.utcnow().isoformat()

            return f"Set preference {key}={value} for user {user_id}"

        def get_profile(self, user_id: str) -> Optional[Dict[str, Any]]:
            """Get user profile as dictionary."""
            if user_id in self._profiles:
                return asdict(self._profiles[user_id])
            return None

        def create_context(
            self,
            session_id: str,
            topic: str = None,
            goals: List[str] = None
        ) -> str:
            """
            Create conversation context.

            Args:
                session_id: Session identifier
                topic: Conversation topic
                goals: List of goals

            Returns:
                Context ID
            """
            context = ConversationContext(
                session_id=session_id,
                topic=topic,
                goals=goals or []
            )
            self._contexts[session_id] = context

            return f"Created context for session {session_id}"

        def add_entity(
            self,
            session_id: str,
            entity_type: str,
            entity_data: Dict[str, Any]
        ) -> str:
            """
            Add an entity to conversation context.

            Args:
                session_id: Session identifier
                entity_type: Type of entity
                entity_data: Entity data

            Returns:
                Confirmation message
            """
            if session_id not in self._contexts:
                self._contexts[session_id] = ConversationContext(session_id=session_id)

            self._contexts[session_id].entities[entity_type] = entity_data

            return f"Added entity {entity_type} to context"

        def get_context(self, session_id: str) -> Optional[Dict[str, Any]]:
            """Get conversation context as dictionary."""
            if session_id in self._contexts:
                return asdict(self._contexts[session_id])
            return None

        def update_sentiment(self, session_id: str, sentiment: str) -> str:
            """Update conversation sentiment."""
            if session_id in self._contexts:
                self._contexts[session_id].sentiment = sentiment
            return f"Updated sentiment to {sentiment}"


    # Tool functions for LangChain
    def tool_collect_user_info(user_id: str, name: str = None, email: str = None) -> str:
        """Collect basic user information."""
        collector = UserInfoCollector()
        return collector.collect_basic_info(user_id, name, email)


    def tool_set_preference(user_id: str, key: str, value: str) -> str:
        """Set a user preference."""
        collector = UserInfoCollector()
        return collector.set_preference(user_id, key, value)


    def tool_get_profile(user_id: str) -> str:
        """Get user profile."""
        collector = UserInfoCollector()
        profile = collector.get_profile(user_id)
        return json.dumps(profile, ensure_ascii=False) if profile else "Profile not found"


    def tool_create_context(session_id: str, topic: str = None) -> str:
        """Create conversation context."""
        collector = UserInfoCollector()
        return collector.create_context(session_id, topic)
    ```
  </action>
</task>

<task type="auto" tdd="true">
  <name>Create tests</name>
  <files>tests/memory/test_user_info_collector.py</files>
</task>

</tasks>

<success_criteria>
- [ ] UserProfile dataclass
- [ ] ConversationContext dataclass
- [ ] UserInfoCollector class
- [ ] Tool functions for LangChain
</success_criteria>

<output>
Create SUMMARY.md
</output>
