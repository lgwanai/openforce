---
phase: 01-security-fixes
plan: 00
type: setup
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/security/__init__.py
  - tests/config/__init__.py
  - tests/security/test_command_execution.py
  - tests/security/test_approval_tokens.py
  - tests/config/test_settings.py
  - tests/security/test_ssrf.py
  - tests/security/test_taint_engine.py
  - pytest.ini
requirements: []
autonomous: true
must_haves:
  truths:
    - "Test framework installed and configured"
    - "Test directory structure exists"
    - "Shared fixtures available for all test files"
  artifacts:
    - path: "pytest.ini"
      provides: "pytest configuration"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
      exports: ["mock_settings", "token_manager", "temp_sandbox"]
---

<objective>
Set up test infrastructure for Phase 1 security fixes.

Purpose: Ensure tests can be written and run for all security requirements.
Output: pytest configuration, test directory structure, shared fixtures.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-security-fixes/01-VALIDATION.md

## Wave 0 Requirements from VALIDATION.md

- [ ] `tests/conftest.py` — shared fixtures
- [ ] `tests/security/__init__.py` — package init
- [ ] `tests/security/test_command_execution.py` — SEC-01 tests
- [ ] `tests/security/test_approval_tokens.py` — SEC-02 tests
- [ ] `tests/config/__init__.py` — package init
- [ ] `tests/config/test_settings.py` — SEC-03 tests
- [ ] `tests/security/test_ssrf.py` — SEC-04 tests
- [ ] `tests/security/test_taint_engine.py` — SEC-05 tests
- [ ] `pytest.ini` — pytest configuration
- [ ] `pip install pytest pytest-cov bandit` — dependencies

</context>

<tasks>

<task type="auto">
  <name>Task 0-01: Create pytest configuration</name>
  <files>pytest.ini</files>
  <action>
Create `pytest.ini`:
```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    security: Security-related tests
    slow: Slow running tests
```
</action>
  <verify>
    <automated>test -f pytest.ini && echo "pytest.ini exists"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 0-02: Create test directory structure</name>
  <files>tests/security/__init__.py, tests/config/__init__.py</files>
  <action>
Create directories and init files:
```bash
mkdir -p tests/security tests/config
touch tests/security/__init__.py
touch tests/config/__init__.py
```
</action>
  <verify>
    <automated>test -d tests/security && test -d tests/config && echo "directories exist"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 0-03: Create shared fixtures</name>
  <files>tests/conftest.py</files>
  <action>
Create `tests/conftest.py`:
```python
import pytest
import tempfile
import os
from pathlib import Path

@pytest.fixture
def temp_sandbox():
    """Create a temporary sandbox directory for file operations."""
    with tempfile.TemporaryDirectory() as tmpdir:
        yield Path(tmpdir)

@pytest.fixture
def mock_settings(monkeypatch):
    """Mock external tools configuration."""
    monkeypatch.setenv('OPENFORCE_BAIDU_SEARCH_SCRIPT', '/tmp/test_search.py')
    monkeypatch.setenv('OPENFORCE_SECURITY_APPROVAL_SECRET_KEY', 'test-secret-key-12345')
    yield

@pytest.fixture
def token_manager():
    """Create a token manager for testing."""
    from src.security.approval import ApprovalTokenManager
    return ApprovalTokenManager(secret_key=b'test-secret-key-for-testing-only')

@pytest.fixture
def command_whitelist():
    """Create a command whitelist for testing."""
    from src.security.command_whitelist import CommandWhitelist
    wl = CommandWhitelist()
    wl.allow('python3')
    wl.allow('echo')
    return wl
```
</action>
  <verify>
    <automated>test -f tests/conftest.py && echo "conftest.py exists"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 0-04: Create test stubs</name>
  <files>tests/security/test_command_execution.py, tests/security/test_approval_tokens.py, tests/config/test_settings.py, tests/security/test_ssrf.py, tests/security/test_taint_engine.py</files>
  <action>
Create test file stubs with placeholder tests:

`tests/security/test_command_execution.py`:
```python
import pytest

class TestCommandExecution:
    """Tests for SEC-01: Shell injection prevention."""
    
    def test_shell_injection_blocked(self):
        """Verify shell injection is blocked."""
        # TODO: Implement after SEC-01 code changes
        pass
    
    def test_whitelist_enforced(self):
        """Verify only whitelisted commands execute."""
        # TODO: Implement after SEC-01 code changes
        pass
```

`tests/security/test_approval_tokens.py`:
```python
import pytest

class TestApprovalTokens:
    """Tests for SEC-02: HMAC token security."""
    
    def test_token_signature_valid(self, token_manager):
        """Verify token signature is valid."""
        # TODO: Implement after SEC-02 code changes
        pass
    
    def test_expired_token_rejected(self, token_manager):
        """Verify expired tokens are rejected."""
        # TODO: Implement after SEC-02 code changes
        pass
    
    def test_timing_attack_resistance(self, token_manager):
        """Verify timing attack resistance."""
        # TODO: Implement after SEC-02 code changes
        pass
```

`tests/config/test_settings.py`:
```python
import pytest

class TestSettings:
    """Tests for SEC-03: Configuration management."""
    
    def test_env_config_loaded(self, mock_settings):
        """Verify environment config is loaded."""
        # TODO: Implement after SEC-03 code changes
        pass
```

`tests/security/test_ssrf.py`:
```python
import pytest

class TestSSRF:
    """Tests for SEC-04: SSRF protection."""
    
    def test_private_ip_blocked(self):
        """Verify private IPs are blocked."""
        # TODO: Implement after SEC-04 code changes
        pass
    
    def test_localhost_blocked(self):
        """Verify localhost is blocked."""
        # TODO: Implement after SEC-04 code changes
        pass
    
    def test_valid_url_allowed(self):
        """Verify valid public URLs are allowed."""
        # TODO: Implement after SEC-04 code changes
        pass
```

`tests/security/test_taint_engine.py`:
```python
import pytest

class TestTaintEngine:
    """Tests for SEC-05: Taint tracking."""
    
    def test_taint_propagation(self):
        """Verify taint propagates correctly."""
        # TODO: Implement after SEC-05 code changes
        pass
    
    def test_high_risk_blocked(self):
        """Verify high-risk tools are blocked."""
        # TODO: Implement after SEC-05 code changes
        pass
    
    def test_sanitization_upgrades_trust(self):
        """Verify sanitization upgrades trust level."""
        # TODO: Implement after SEC-05 code changes
        pass
```
</action>
  <verify>
    <automated>pytest tests/ --collect-only 2>&1 | head -20</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 0-05: Update requirements</name>
  <files>requirements.txt</files>
  <action>
Add to `requirements.txt` if not present:
```
pytest>=7.0.0
pytest-cov>=4.0.0
bandit>=1.7.0
pydantic-settings>=2.0.0
```

Install:
```bash
pip install pytest pytest-cov bandit pydantic-settings
```
</action>
  <verify>
    <automated>pip show pytest && pip show bandit && echo "dependencies installed"</automated>
  </verify>
</task>

</tasks>

<verification>

### Must-Haves Verification

- [ ] pytest.ini exists
- [ ] tests/security/ directory exists
- [ ] tests/config/ directory exists
- [ ] tests/conftest.py exists with fixtures
- [ ] All test stub files exist
- [ ] pytest can collect tests

### Test Commands

```bash
# Verify test infrastructure
pytest tests/ --collect-only

# Run empty tests (should pass)
pytest tests/ -v
```

</verification>

<success_criteria>
1. pytest installed and configured
2. Test directory structure exists
3. All test files created with stubs
4. pytest can discover tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-fixes/01-00-SUMMARY.md`
</output>
