---
phase: 01-security-fixes
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/security/__init__.py
  - tests/security/test_command_execution.py
  - tests/security/test_approval_tokens.py
  - tests/security/test_ssrf.py
  - tests/security/test_taint_engine.py
  - tests/config/__init__.py
  - tests/config/test_settings.py
  - tests/tools/__init__.py
  - tests/tools/test_base.py
  - pytest.ini
requirements: []  # Infrastructure only
autonomous: true
must_haves:
  truths:
    - "Test infrastructure exists and runs"
    - "All test files have basic scaffolding"
    - "Fixtures are available for tests"
  artifacts:
    - path: "pytest.ini"
      provides: "pytest configuration"
    - path: "tests/conftest.py"
      provides: "Shared fixtures"
  key_links: []
---

<objective>
Create test infrastructure and scaffolding for all Phase 1 security tests.

Purpose: Enable test-driven development for security fixes.
Output: Test files, fixtures, and pytest configuration.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-fixes/01-VALIDATION.md

## Wave 0 Requirements

From VALIDATION.md Wave 0 Requirements:
- [ ] `tests/conftest.py` — shared fixtures
- [ ] `tests/security/__init__.py` — package init
- [ ] `tests/security/test_command_execution.py` — SEC-01 tests
- [ ] `tests/security/test_approval_tokens.py` — SEC-02 tests
- [ ] `tests/config/__init__.py` — package init
- [ ] `tests/config/test_settings.py` — SEC-03 tests
- [ ] `tests/security/test_ssrf.py` — SEC-04 tests
- [ ] `tests/security/test_taint_engine.py` — SEC-05 tests
- [ ] `pytest.ini` — pytest configuration

</context>

<tasks>

<task type="auto">
  <name>Task 0-01: Create pytest configuration</name>
  <files>pytest.ini</files>
  <action>
Create `pytest.ini` in project root:

```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
```
</action>
  <verify>
    <automated>test -f pytest.ini && echo "pytest.ini exists"</automated>
  </verify>
  <done>
    - pytest.ini created
    - Test discovery configured
  </done>
</task>

<task type="auto">
  <name>Task 0-02: Create test directory structure</name>
  <files>tests/security/__init__.py, tests/config/__init__.py, tests/tools/__init__.py</files>
  <action>
Create test package directories:

```bash
mkdir -p tests/security tests/config tests/tools
touch tests/security/__init__.py
touch tests/config/__init__.py
touch tests/tools/__init__.py
```

Each `__init__.py` can be empty or contain:
```python
"""Tests for [module name]."""
```
</action>
  <verify>
    <automated>test -d tests/security && test -d tests/config && test -d tests/tools && echo "Directories exist"</automated>
  </verify>
  <done>
    - tests/security/ directory exists
    - tests/config/ directory exists
    - tests/tools/ directory exists
    - All __init__.py files created
  </done>
</task>

<task type="auto">
  <name>Task 0-03: Create shared fixtures in conftest.py</name>
  <files>tests/conftest.py</files>
  <action>
Create `tests/conftest.py`:

```python
"""
Shared fixtures for OpenForce tests.
"""

import pytest
import os
from unittest.mock import MagicMock, patch
from pathlib import Path


@pytest.fixture
def mock_settings():
    """Mock ExternalToolsConfig for testing."""
    with patch('config.settings.ExternalToolsConfig') as mock:
        config = MagicMock()
        config.baidu_search_script = Path("/fake/path/search.py")
        config.agent_browser_path = "/usr/local/bin/agent-browser"
        config.playwright_cache_dir = Path("/tmp/Library/Caches/ms-playwright")
        config.user_home = Path("/home/testuser")
        mock.return_value = config
        yield config


@pytest.fixture
def mock_security_config():
    """Mock SecurityConfig for testing."""
    with patch('config.settings.SecurityConfig') as mock:
        config = MagicMock()
        config.approval_secret_key = "test-secret-key-for-testing-only"
        config.approval_token_expires_in = 3600
        mock.return_value = config
        yield config


@pytest.fixture
def mock_token_manager(mock_security_config):
    """Mock ApprovalTokenManager for testing."""
    from security.taint_engine import ApprovalTokenManager
    return ApprovalTokenManager(secret_key=b"test-secret-key-32-bytes-for-testing!")


@pytest.fixture
def mock_whitelist():
    """Mock CommandWhitelist for testing."""
    from security.command_whitelist import CommandWhitelist
    whitelist = CommandWhitelist()
    whitelist.allow('python3')
    whitelist.allow('npx')
    return whitelist


@pytest.fixture
def clean_env(monkeypatch):
    """Clean environment variables for isolated tests."""
    # Remove any OPENFORCE_ prefixed variables
    for key in list(os.environ.keys()):
        if key.startswith('OPENFORCE_'):
            monkeypatch.delenv(key, raising=False)
```
</action>
  <verify>
    <automated>test -f tests/conftest.py && echo "conftest.py exists"</automated>
  </verify>
  <done>
    - tests/conftest.py created
    - Fixtures defined for common test scenarios
  </done>
</task>

<task type="auto">
  <name>Task 0-04: Create test scaffolds for SEC-01</name>
  <files>tests/security/test_command_execution.py</files>
  <action>
Create `tests/security/test_command_execution.py`:

```python
"""
Tests for SEC-01: Shell Injection Prevention.

Tests that command execution uses whitelist and no shell=True.
"""

import pytest
from unittest.mock import patch, MagicMock


class TestCommandWhitelist:
    """Tests for CommandWhitelist class."""
    
    def test_shell_injection_blocked(self):
        """Test that shell injection attempts are blocked."""
        # TODO: Implement after CommandWhitelist is created
        # This should verify that commands like "; rm -rf" are treated as literal args
        pass
    
    def test_whitelist_enforced(self):
        """Test that non-whitelisted executables are blocked."""
        # TODO: Implement after CommandWhitelist is created
        pass
    
    def test_allowed_command_executes(self):
        """Test that whitelisted commands execute successfully."""
        # TODO: Implement after CommandWhitelist is created
        pass


class TestRunAgentBrowser:
    """Tests for run_agent_browser function."""
    
    def test_uses_safe_executor(self):
        """Test that run_agent_browser uses safe executor."""
        # TODO: Implement after safe executor is created
        pass
    
    def test_no_shell_true(self):
        """Test that no shell=True is used in execution."""
        # TODO: Verify no shell=True in codebase
        pass
```
</action>
  <verify>
    <automated>test -f tests/security/test_command_execution.py && echo "File exists"</automated>
  </verify>
  <done>
    - Test scaffold for SEC-01 created
    - Test class structure defined
  </done>
</task>

<task type="auto">
  <name>Task 0-05: Create test scaffolds for SEC-02</name>
  <files>tests/security/test_approval_tokens.py</files>
  <action>
Create `tests/security/test_approval_tokens.py`:

```python
"""
Tests for SEC-02: Approval Token Security.

Tests that tokens are HMAC-signed and timing-attack resistant.
"""

import pytest
import time


class TestApprovalTokenManager:
    """Tests for ApprovalTokenManager class."""
    
    def test_token_signature_valid(self):
        """Test that valid tokens are verified correctly."""
        # TODO: Implement after ApprovalTokenManager is created
        pass
    
    def test_expired_token_rejected(self):
        """Test that expired tokens are rejected."""
        # TODO: Implement after ApprovalTokenManager is created
        pass
    
    def test_wrong_secret_rejected(self):
        """Test that tokens signed with wrong key are rejected."""
        # TODO: Implement after ApprovalTokenManager is created
        pass
    
    def test_timing_attack_resistance(self):
        """Test that verification uses constant-time comparison."""
        # TODO: Implement timing test
        pass


class TestTokenFunctions:
    """Tests for generate_approval_token and verify_approval_token functions."""
    
    def test_backwards_compatible_signature(self):
        """Test that old function signatures still work."""
        # TODO: Verify backwards compatibility
        pass
```
</action>
  <verify>
    <automated>test -f tests/security/test_approval_tokens.py && echo "File exists"</automated>
  </verify>
  <done>
    - Test scaffold for SEC-02 created
    - Test class structure defined
  </done>
</task>

<task type="auto">
  <name>Task 0-06: Create test scaffolds for SEC-03</name>
  <files>tests/config/test_settings.py, tests/tools/test_base.py</files>
  <action>
Create `tests/config/test_settings.py`:

```python
"""
Tests for SEC-03: Configuration Externalization.

Tests that paths are loaded from environment, not hardcoded.
"""

import pytest
import os


class TestSecurityConfig:
    """Tests for SecurityConfig."""
    
    def test_env_config_loaded(self):
        """Test that environment variables are loaded."""
        # TODO: Implement after SecurityConfig is created
        pass
    
    def test_default_secret_key_generated(self):
        """Test that secret key is generated if not set."""
        # TODO: Implement after SecurityConfig is created
        pass


class TestExternalToolsConfig:
    """Tests for ExternalToolsConfig."""
    
    def test_path_from_env(self):
        """Test that paths are loaded from environment."""
        # TODO: Implement after ExternalToolsConfig is created
        pass
```

Create `tests/tools/test_base.py`:

```python
"""
Tests for src/tools/base.py functions.
"""

import pytest


class TestNoHardcodedPaths:
    """Tests to verify no hardcoded paths in codebase."""
    
    def test_no_hardcoded_paths(self):
        """Test that no /Users/wuliang paths exist in tools."""
        # TODO: Implement path detection
        pass
```
</action>
  <verify>
    <automated>test -f tests/config/test_settings.py && test -f tests/tools/test_base.py && echo "Files exist"</automated>
  </verify>
  <done>
    - Test scaffolds for SEC-03 created
    - Test class structures defined
  </done>
</task>

<task type="auto">
  <name>Task 0-07: Create test scaffolds for SEC-04</name>
  <files>tests/security/test_ssrf.py</files>
  <action>
Create `tests/security/test_ssrf.py`:

```python
"""
Tests for SEC-04: SSRF Protection.

Tests that internal resources are blocked from fetch_webpage.
"""

import pytest


class TestSSRFProtection:
    """Tests for SSRF validation."""
    
    def test_private_ip_blocked(self):
        """Test that private IP ranges are blocked."""
        # TODO: Implement after SSRF module is created
        pass
    
    def test_localhost_blocked(self):
        """Test that localhost is blocked."""
        # TODO: Implement after SSRF module is created
        pass
    
    def test_valid_url_allowed(self):
        """Test that valid public URLs are allowed."""
        # TODO: Implement after SSRF module is created
        pass


class TestFetchWebpage:
    """Tests for fetch_webpage_safe function."""
    
    def test_ssrf_blocked_returns_error_message(self):
        """Test that blocked URLs return error message."""
        # TODO: Implement after SSRF protection is added
        pass
```
</action>
  <verify>
    <automated>test -f tests/security/test_ssrf.py && echo "File exists"</automated>
  </verify>
  <done>
    - Test scaffold for SEC-04 created
    - Test class structure defined
  </done>
</task>

<task type="auto">
  <name>Task 0-08: Create test scaffolds for SEC-05</name>
  <files>tests/security/test_taint_engine.py</files>
  <action>
Create `tests/security/test_taint_engine.py`:

```python
"""
Tests for SEC-05: Taint Tracking Implementation.

Tests for TaintedValue, TaintEngine, and taint propagation.
"""

import pytest


class TestTaintedValue:
    """Tests for TaintedValue class."""
    
    def test_taint_propagation(self):
        """Test that taint propagates through transformations."""
        # TODO: Implement after TaintedValue is created
        pass
    
    def test_trust_level_derived(self):
        """Test that trust level is derived from sources."""
        # TODO: Implement after TaintedValue is created
        pass


class TestTaintEngine:
    """Tests for TaintEngine class."""
    
    def test_high_risk_blocked(self):
        """Test that high-risk tools are blocked."""
        # TODO: Implement after TaintEngine is updated
        pass
    
    def test_sanitization_upgrades_trust(self):
        """Test that sanitization upgrades trust level."""
        # TODO: Implement after sanitize method is added
        pass
```
</action>
  <verify>
    <automated>test -f tests/security/test_taint_engine.py && echo "File exists"</automated>
  </verify>
  <done>
    - Test scaffold for SEC-05 created
    - Test class structure defined
  </done>
</task>

</tasks>

<verification>

### Must-Haves Verification

- [ ] pytest.ini exists
- [ ] tests/conftest.py with fixtures
- [ ] tests/security/__init__.py exists
- [ ] tests/config/__init__.py exists
- [ ] tests/tools/__init__.py exists
- [ ] All test scaffold files created
- [ ] pytest can discover tests

### Test Commands

```bash
# Verify pytest can discover tests
pytest --collect-only tests/

# Verify pytest configuration
pytest --version

# Run empty tests (should pass)
pytest tests/ -v
```

</verification>

<success_criteria>
1. Test infrastructure fully created
2. All test files exist with scaffolding
3. pytest can discover and run tests
4. Fixtures available for use
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-fixes/00-00-SUMMARY.md`
</output>
