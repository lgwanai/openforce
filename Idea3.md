/gsd:add-phase 1、用户回收信息，当做是一个tool存在，不要让户部在传信息给zhongshu，zhongshu在问用户，如  
  果户部发起用户询问，通过tool收集到的信息直接给户部，就不需要绕一大圈，而且没有错误收集信息的错误。      
  2、户部的记忆需要重新设计，现在第一次问题天气怎样，他收集一遍城市，我再一次问天气怎样，又问一遍城市。   
  正常预期：                                                                                              
  - zhongshu只保存会话窗口有限的上下文信息，超过上下文条数或者token数，就将所有上下文交给户部，换成压缩   
  后的上下文                                                                                              
  - hubu 需要管理所有记忆，包括短期和长期。短期记忆就是管理session下的上下文，户部需要记录session下每一
  轮用户输入和最终输出原文，以markdown格式存储。
  - hubu还需要提供压缩上下文能力，当zhongshu上下文过载，就会要求hubu将对应沟通轮次区间压缩成信息摘要，替
  换zhongshu真正的上下文，这个动作用户是无感的。这个压缩能力hubu需要有自己的策略主动做，比如每隔20条就压
  缩一次，并以文件形式存下来，跟session原文要有关联，一段一个文件，这样方便检索。
  - hubu还需要存储所有从互联网访问的所有信息，markdown格式保存，作为原文存储。
  - 当前端用户输入/dream指令，hubu需要开始整理所有未整理过的原文，原文内容除了互联网抓取，还有用户上传的
  各种文档、表格、图片都属于原文。处理的方式是总结归纳成知识点，用图数据库存储下来，形成网状结构。知识点
  需要有原文属性。方便后续归因出处。
  - 当一次用户指令需要调度hubu时，hubu不能直接启动任务分配抓取网页，而是通过并行的获取短期记忆（上下文相
  关信息）、图数据库语义向量搜索（已经归纳过的原文）、使用关键词检索（未归纳的原文），这三个并行获取信息
  ，相关提取后，信息权重短期记忆权重最高、然后是图数据库检索、最后是原文检索，如果出现冲突，以权限最高的
  为准，信息如果过载就需要使用重排序模型，进行rerank处理，最终返回可信度最高的头部几条。
  - 当用户反馈信息不对时，相关所有信息需要做标记，在后续检索中需要降低权重
  - 在存储信息的时候需要使用模型洞察信息是否跟时间紧密相关，是否有过期时效，如果有，需要标记。比如天气信
  息，仅当日有效，第二天在问今天天气的时候，检索需要佩戴时间，一方面发现日期不对会被抛弃，另一方面即便日
  期被篡改，有过期时间戳，依然可以作为依据排除。
  请充分系统性设计三层记忆系统